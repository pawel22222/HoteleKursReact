import styles from './aboutUs.module.scss'

function AboutUs() {
    return (
        <div className={ `${styles.aboutUstDiv}` }>
            <h1 className={ `${styles.header}` }>O nas</h1>
        </div>
    )
}

export default AboutUs