import styles from './Contakt.module.scss'

function Contakt() {
    return (
        <div className={ `${styles.contaktDiv}` }>

            <h1 className={ `${styles.header}` }>Kontakt</h1>
        </div>
    )
}

export default Contakt